#include <algorithm>
#include <iostream>
#include <sstream>
#include <cstdlib>
#include <cstring>
#include <cstdio>
#include <string>
#include <vector>
#include <queue>
#include <cmath>
#include <map>
#include <set>
using namespace std;

#define FOR(i, n) for (int i = 0; i < (n); ++i)

template<class T> void output(T _array, int _n) {
    FOR(_i, _n) cout << _array[_i] << " "; cout << endl;
}
template<class T> void output(T _array, int _n, int _m) {
    FOR(_i, _n) output(_array[_i], _m);
}

int main() {
#ifdef DEBUG
    freopen("input", "r", stdin);
#endif

    return 0;
}
